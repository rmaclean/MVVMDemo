<Page
    x:Class="MVVMDemo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MVVMDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" RequestedTheme="Light">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="0,50,0,0">
            <TextBox x:Name="textBox" TextWrapping="Wrap" Text="{Binding Name, Mode=OneWay}"/>
            <Button x:Name="button" Content="Button" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Command="{Binding Add}"/>
            <ListView x:Name="listView" Height="333" ItemsSource="{Binding Items}"/>
        </StackPanel>

    </Grid>
</Page>
